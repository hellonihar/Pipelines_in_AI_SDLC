# requirements.txt — retained for pip compatibility
# UV users: run `uv sync` or `uv pip install -r requirements.txt`
# Preferred: use pyproject.toml (`uv sync` reads dependencies from there)

pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
matplotlib>=3.7.0
flask>=2.3.0
